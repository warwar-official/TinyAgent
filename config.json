{
    "tools": [
        {
            "name": "get_current_weather",
            "description": "Gets the current weather for a given location.",
            "parameters": {
                "properties": {
                    "location": {
                        "type": "string",
                        "description": "The city name, e.g. Lviv"
                    }
                },
                "required": [
                    "location"
                ]
            }
        },
        {
            "name": "web_search",
            "description": "Searches the web for a given query.",
            "parameters": {
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "The query to search for"
                    },
                    "count": {
                        "type": "integer",
                        "description": "The number of results to return. Default: 3, Max: 5"
                    }
                },
                "required": [
                    "query"
                ]
            }
        },
        {
            "name": "fetch_url",
            "description": "Fetches the content of a URL, returning clean text, links, and table data.",
            "parameters": {
                "properties": {
                    "url": {
                        "type": "string",
                        "description": "The URL to fetch content from."
                    }
                },
                "required": [
                    "url"
                ]
            }
        },
        {
            "name": "send_message",
            "description": "Sends a message to a user interface.",
            "parameters": {
                "properties": {
                    "message": {
                        "type": "string",
                        "description": "The message to send."
                    }
                },
                "required": [
                    "message"
                ]
            }
        },
        {
            "name": "read_file",
            "description": "Reads a file.",
            "parameters": {
                "properties": {
                    "path": {
                        "type": "string",
                        "description": "The path to the file."
                    }
                },
                "required": [
                    "path"
                ]
            }
        },
        {
            "name": "write_file",
            "description": "Writes a file.",
            "parameters": {
                "properties": {
                    "path": {
                        "type": "string",
                        "description": "The path to the file."
                    },
                    "content": {
                        "type": "string",
                        "description": "The content to write to the file."
                    }
                },
                "required": [
                    "path",
                    "content"
                ]
            }
        }
    ]
}